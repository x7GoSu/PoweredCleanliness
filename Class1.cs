using PoweredCleanliness;
using Verse;

public class CompProperties_PowerCleanliness : CompProperties
{
    public CompProperties_PowerCleanliness()
    {
        compClass = typeof(CompPowerCleanliness);
    }
}